server:

logs:
  folder: 
  prefix:       

# watched files
files:
  # path: path of file
  # pattern for new line (default is \n)
  # color: color of the line (default auto)
  # color can be one of: blue, green, red, yellow, black, grey, pink, orange
  - 
    path: /var/log/syslog
    pattern: 
    color:  
  - 
    path: /var/log/Xorg.0.log
    pattern:  \(Entry .+\)
    color: blue 

